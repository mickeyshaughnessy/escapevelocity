{
  "name": "@escapevelocity/client",
  "version": "1.0.0",
  "description": "Escape Velocity game client",
  "scripts": {
    "build": "parcel build src/index.html --dist-dir dist --public-url ./",
    "dev": "parcel src/index.html --dist-dir dist --port 8080"
  },
  "dependencies": {
    "@escapevelocity/shared": "*",
    "socket.io-client": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "parcel": "^2.10.3",
    "typescript": "^5.3.3"
  }
}
